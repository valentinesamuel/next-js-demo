const FriendsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <section>
      <p>This is a friends layout</p>
      {children}
    </section>
  );
};

export default FriendsLayout;
